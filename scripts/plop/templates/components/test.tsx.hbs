import { render, screen } from '@testing-library/react'

import { Sample } from './{{ pascalCase name }}.stories'

describe('{{ pascalCase type }}/{{ pascalCase name }}', () => {
  it('should renders elements correctly', () => {
    render(<Sample {...Sample.args} />)

    expect(screen.getByText(/Created {{ pascalCase name }}! content test/gi)).toBeInTheDocument()
  })
})
