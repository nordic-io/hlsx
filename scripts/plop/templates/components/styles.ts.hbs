import { useStyles } from '@utils/helpers'

const use{{ pascalCase name }}Styles = () =>
  useStyles(theme => ({
    root: {
      backgroundColor: theme.colors.rewardsTier2
    }
  }))

export default use{{ pascalCase name }}Styles
